# FastAPI LINE Bot (Database Query)

這是一個基於 FastAPI 撰寫的 LINE Bot。

## 🛠️ 開發環境準備

1. **環境變數設定**：
   在專案根目錄建立 `.env` 檔案，並填入以下資訊：
   ```env
   LINE_CHANNEL_ACCESS_TOKEN=你的_Token
   LINE_CHANNEL_SECRET=你的_Secret